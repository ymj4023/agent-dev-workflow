# Issue Tracker：项目内 Markdown

当前 issue 与规格位于 `.scratch/<feature>/`：`spec.md` 表示 Destination，`issues/<NN>-<slug>.md` 表示按 blocker 优先排序的 Journey ticket。每个 ticket 记录 `Status`、`Blocked by`、可观察交付物和接受标准。

需要切换到 GitHub Issues 时，应先明确迁移边界，再用 Matt 对应的 tracker 契约替换本文件；不得同时维护两个 canonical tracker。
