# 05 - 映射远程执行且不创建 Control Plane

**交付内容：** 可选 GitHub `gh-aw`、GitHub Actions/webhook、Symphony 与 Multica adapter，把项目 ticket 转换成隔离运行，同时把工作流状态留在项目 tracker。

**Blocked by:** 04 - 安装工作流资产且不覆盖用户内容（已完成）。

**Status:** ready-for-agent

- [ ] 每个 adapter 映射相同 Journey ticket 与证据契约。
- [ ] 所有远程 adapter 缺失时核心测试仍通过。
- [ ] Multica 只是 adapter，绝不作为 canonical 资产仓库。
