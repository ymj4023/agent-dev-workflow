# 证据规则

- Gate 只接受可检查证据：命令输出、测试结果、diff、review finding、截图或显式人工决策。
- 记录准确命令和结果。没有运行的命令保持 `UNVERIFIED`；跳过的检查必须说明原因与风险。
- 分开记录事实、推断与偏好。未知信息保持 `UNKNOWN`，不得包装成确定结论。
- RED 证据必须证明目标行为因预期原因失败；最小实现后用同一反馈环取得 GREEN 证据。
- 完成报告必须列出改动文件、已运行检查、失败、跳过项和剩余人工决策。
- 证据保存或共享前必须移除凭据、个人数据和本机环境信息。
